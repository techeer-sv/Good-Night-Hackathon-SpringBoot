spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql:3306/techeerdb
    username: root
    password: mysql0824

  jpa:
    hibernate:
      ddl-auto : create #update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true    # ???? JPQL? ????? ??? ?? ????.
    show_sql: true

logging:
  level:
    org:
      hibernate: info